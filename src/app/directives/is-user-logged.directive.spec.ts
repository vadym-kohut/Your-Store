import { IsUserLoggedDirective } from './is-user-logged.directive';

describe('IsUserLoggedDirective', () => {
    it('should create an instance', () => {
        const directive = new IsUserLoggedDirective();
        expect(directive).toBeTruthy();
    });
});
